{
  "name": "mainbit",
  "version": "1.0.0",
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "dev": "parcel serve src/pages/*.pug",
    "build": "parcel build src/pages/*.pug --no-cache --no-content-hash --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@babel/core": "^7.23.7",
    "@parcel/transformer-pug": "^2.11.0",
    "@parcel/transformer-sass": "^2.11.0",
    "parcel": "^2.11.0",
    "pug": "^3.0.2",
    "sass": "^1.70.0"
  },
  "browserslist": [
    "last 1 chrome version"
  ],
  "dependencies": {
    "bootstrap": "^4.6.2",
    "swiper": "^11.0.5"
  }
}
